<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/hell_grey">

    <TextView
        android:id="@+id/theme_change_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_margin="20dp"
        android:layout_marginTop="30dp"
        android:background="@color/pinky"
        android:text="@string/theme_selection_text_view"
        android:textSize="40sp"
        android:textColor="@color/dark_green"
        />

    <RadioGroup
        android:id="@+id/select_theme_radiobutton_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        app:layout_constraintTop_toBottomOf="@id/theme_change_text_view">

        <com.google.android.material.radiobutton.MaterialRadioButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/radioButtonsStyle"
            android:text="@string/my_theme_button_text"
            />

        <com.google.android.material.radiobutton.MaterialRadioButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/radioButtonsStyle"
            android:text="@string/main_theme_button_text"
            />

    </RadioGroup>

    <com.google.android.material.button.MaterialButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/select_theme_radiobutton_group"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_margin="50dp"
        android:backgroundTint="@color/dark_green"
        android:textColor="@color/pinky"
        android:textSize="22sp"
        app:cornerRadius="14dp"
        android:text="@string/return_button"/>

</androidx.constraintlayout.widget.ConstraintLayout>